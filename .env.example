# Convex (run `npx convex dev` to populate these automatically)
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Add to Convex dashboard environment variables (Settings → Environment Variables):
# ANTHROPIC_API_KEY=sk-ant-...
